# 协作规则

本项目后续修改，默认遵守以下规则。

## 1. 修改原则

- 优先使用 `apply_patch` 修改现有文件。
- 不直接通过终端重定向中文内容到 `html`、`js`、`py`、`css` 文件。
- 不顺手修改无关页面、无关模块、无关文案。
- 一次只处理一个页面、一个模块或一类问题，先小范围改动，再验证。

## 2. 编码规则

- 所有源码文件统一按 `UTF-8` 处理。
- Python 读写文本文件时，必须显式写 `encoding="utf-8"`。
- 如果必须使用临时脚本修改文件，优先写成 UTF-8 脚本文件后再执行。
- 如果终端链路可能污染中文，允许使用 Unicode 转义，避免中文直接经过命令行参数。

## 3. 乱码防护

- 修改前，先扫描目标文件是否存在乱码、残缺字符串、异常引号、异常标签。
- 修改后，立即扫描目标文件是否存在 `??`、乱码特征串、明显损坏片段。
- 如果文件已经被乱码污染，不在脏片段上反复碎补丁。
- `apply_patch` 无法稳定命中时，先说明原因，再改为整段替换或整函数替换。

## 4. 前端文件特殊规则

- 不使用终端 here-string 或输出重定向直接改写 `templates/*.html`、`static/*.js`、`static/*.css`。
- 前端文案修改后，应提示用户执行浏览器强刷：`Ctrl + F5`。
- 如果用户只要求改某一个页面，只允许改该页面及其直接依赖的最小代码范围。

## 5. 工作流程

每次任务默认按下面顺序执行：

1. 定位目标文件和目标区域。
2. 扫描乱码和结构异常。
3. 优先用 `apply_patch` 做最小修改。
4. 如果补丁失败，先解释失败原因，再采用 UTF-8 临时脚本或整段替换。
5. 修改后再次扫描乱码。
6. 告诉用户当前是计划的哪一步、如何启动、如何验证、预期输出是什么。

## 6. 用户协作说明

当用户要求修改时，默认按以下要求执行：

- 先告诉用户当前属于计划的哪一步。
- 告诉用户本步的前置检查项。
- 给出可直接执行的命令。
- 说明预期输出或预期页面效果，方便用户核对。

## 7. 禁止事项

- 禁止为了快，直接把中文文本通过 PowerShell 或 cmd 输出重定向覆盖源码文件。
- 禁止在乱码文件上不做检查就连续多轮修改。
- 禁止在一次任务里同时大改 UI、逻辑、日志、接口，除非用户明确要求。
