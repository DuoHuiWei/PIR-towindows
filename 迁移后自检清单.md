# 迁移后自检清单

## 适用场景

- 项目已经整体拷贝到新电脑
- 不改代码
- 不重新编译 Rust
- 直接使用现成 `.exe`

---

## 第 1 步：确认项目根目录路径

### 前置检查

- 先确认项目在新电脑上的实际位置

### 示例

- `D:\PIR\new-work-v1`
- `E:\Projects\new-work-v1`

### 预期结果

- 后续所有检查命令都基于这个目录执行

---

## 第 2 步：确认 3 处路径已修改

### 检查文件

- `config.py`
- `rust_project/config/pirex.json`
- `rust_project/config/pirexx.json`

### 检查目标

- 文件里不应再出现旧路径：

```text
E:\pir-worktrees\new-work-v1
```

- 应该全部改成新电脑上的实际项目根目录

### 预期结果

- Python 和 Rust 运行时都会指向新目录

---

## 第 3 步：检查目录结构是否完整

### 命令

```cmd
cd /d D:\PIR\new-work-v1
dir
dir rust_project
dir rust_data
```

### 预期结果

- 根目录下至少能看到：
  - `rust_project`
  - `rust_data`
  - `client_ops`
  - `server_ops`
  - `static`
  - `templates`
  - `config.py`
  - `main.py`

---

## 第 4 步：检查 Rust 配置文件是否存在

### 命令

```cmd
cd /d D:\PIR\new-work-v1
dir rust_project\config
```

### 预期结果

- 至少应看到：
  - `pirex.json`
  - `pirexx.json`

---

## 第 5 步：检查 Rust 可执行文件是否存在

### 命令

```cmd
cd /d D:\PIR\new-work-v1
dir rust_project\target\x86_64-pc-windows-gnu\debug
```

### 预期结果

- 至少应看到：
  - `pirex_sprep.exe`
  - `pirex_uprep.exe`
  - `pirex_sread.exe`
  - `pirex_uread.exe`
  - `pirexx_sprep.exe`
  - `pirexx_uprep.exe`
  - `pirexx_sread.exe`
  - `pirexx_uread.exe`
  - `helper.exe`

### 说明

- 如果你的实际运行目录不是 `debug`，就按实际目录检查

---

## 第 6 步：检查 dlp.bin 是否存在

### 命令

```cmd
cd /d D:\PIR\new-work-v1
dir rust_project\utils
```

### 预期结果

- 应看到：
  - `dlp.bin`

### 说明

- `pirexx` 运行时需要它
- 没有这个文件，`pirexx` 通常无法正常完成加解密相关流程

---

## 第 7 步：检查数据根目录是否存在

### 命令

```cmd
cd /d D:\PIR\new-work-v1
dir rust_data\server-data
```

### 预期结果

- 应能看到数据库目录，例如：
  - `test4096`
  - `test16384`
  - `test65536`
  - 或你自己的业务数据库目录

---

## 第 8 步：检查某个数据库是否完整

### 命令示例

```cmd
cd /d D:\PIR\new-work-v1
dir rust_data\server-data\test4096
dir rust_data\server-data\test4096\snapshot
```

### 预期结果

- 数据库目录下至少应看到：
  - `snapshot`
  - `state-pirex` 或 `state_pirex`
  - `state-pirexx` 或 `state_pirexx`

- `snapshot` 下至少应看到：
  - `data.bin`

---

## 第 9 步：做一次 pirex 最小启动验证

### 命令

```cmd
cd /d D:\PIR\new-work-v1\rust_project
target\x86_64-pc-windows-gnu\debug\pirex_sprep.exe test4096 127.0.0.1:18112
```

### 预期输出

```text
sprep: listening on 127.0.0.1:18112
```

### 说明

- 只要能监听成功，就说明：
  - exe 可运行
  - `pirex.json` 生效
  - 路径没有明显错位

### 验证后操作

- 按 `Ctrl + C` 停止即可

---

## 第 10 步：做一次 pirexx 最小启动验证

### 命令

```cmd
cd /d D:\PIR\new-work-v1\rust_project
target\x86_64-pc-windows-gnu\debug\pirexx_sprep.exe test4096 127.0.0.1:18114
```

### 预期输出

```text
sprep: listening on 127.0.0.1:18114
```

### 说明

- 只验证能启动
- 不要求当场跑完整预处理

### 验证后操作

- 按 `Ctrl + C` 停止即可

---

## 第 11 步：检查 Python 服务是否能启动

### 命令

```cmd
cd /d D:\PIR\new-work-v1
python main.py
```

### 预期结果

- 不应直接因为路径问题报错
- 如果报错，优先检查：
  - `config.py`
  - `rust_data`
  - `templates`
  - `static`

---

## 常见迁移失败点

### 1. 旧路径没改干净

现象：

- 仍然访问 `E:\pir-worktrees\new-work-v1`

优先检查：

- `config.py`
- `pirex.json`
- `pirexx.json`

### 2. 缺少 dlp.bin

现象：

- `pirexx` 相关流程启动后异常

优先检查：

- `rust_project/utils/dlp.bin`

### 3. 只拷了 rust_project，没拷完整项目

现象：

- Python 页面、日志、数据目录、数据库目录异常

优先检查：

- 是否整个项目目录一起迁移

### 4. exe 目录不对

现象：

- 启动时提示找不到 `.exe`

优先检查：

- `rust_project/target/x86_64-pc-windows-gnu/debug`

---

## 一句话结论

迁移到新电脑后，先检查：

1. 3 处根路径是否改对
2. exe 是否存在
3. `dlp.bin` 是否存在
4. `rust_data/server-data` 是否完整
5. `pirex_sprep.exe` / `pirexx_sprep.exe` 是否能最小启动

如果这 5 项都通过，通常说明迁移基本成功。
