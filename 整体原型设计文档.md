# 双端 PIR 文件检索原型设计文档

## 1. 项目目标

本项目要开发一个客户端 / 服务器端双端 PIR 文件检索软件原型。

当前阶段目标：

- 使用 Rust 保留并运行 PIR 核心协议
- 使用 Python FastAPI 承担控制层、文件流、业务流
- 使用本地浏览器页面作为客户端前端入口
- 先在同一台本机上完成原型联调
- 原型跑通后，再拆分部署到两台远程机器

## 2. 技术栈

- Rust：核心 PIR 协议
- Python FastAPI：客户端 API、服务端 API、业务控制层
- 前端：纯 HTML + CSS + JavaScript

前端不使用 Vue、React、jQuery，仅负责：

- 展示页面
- 提交表单
- 上传文件
- 展示查询结果
- 触发文件下载

## 3. 当前部署方式

当前原型阶段：

- 客户端 FastAPI：`127.0.0.1:8000`
- 服务端 FastAPI：`127.0.0.1:8001`
- 两个 FastAPI 实例暂时写在同一个 Python 主程序中
- Rust 客户端、Rust 服务端程序也都在同一台本机调试

后续再拆分为两台不同机器部署。

## 4. 系统分层

### 4.1 Rust 层

Rust 只负责 PIR 核心协议，不承担业务页面、文件管理、用户管理职责。

客户端侧：

- `pirexx_uprep`
- `pirexx_uread`

服务端侧：

- `pirexx_sprep`
- `pirexx_sread`

Rust 之间继续使用现有加密 TCP 协议，不修改核心收发逻辑。

### 4.2 Python 层

Python 负责：

- 启动和回收 Rust 进程
- 文件上传与分发
- 打包与恢复流程控制
- manifest 管理
- 数据库管理
- 用户登录与用户管理
- 日志记录
- 浏览器下载响应

### 4.3 前端层

浏览器只连接客户端 FastAPI，不直接访问服务端 FastAPI，也不直接接触 Rust。

## 5. 三条通路

### 通路 1：浏览器 ↔ 客户端 Python

用途：

- 登录
- 上传文件
- 数据库选择
- 查询文件
- 触发恢复
- 下载恢复结果

协议：

- HTTP / FastAPI

### 通路 2：客户端 Rust ↔ 服务端 Rust

用途：

- 预处理协议
- 在线 PIR 查询协议
- 块恢复

协议：

- 现有加密 TCP

### 通路 3：客户端 Python ↔ 服务端 Python

用途：

- 上传待预处理文件到服务端
- 触发服务端打包
- 触发预处理
- 获取 manifest
- 管理数据库与日志

协议：

- HTTP / FastAPI

## 6. 页面结构

### 6.1 登录界面

系统刚启动时进入登录界面。

要求：

- 用户登录
- 管理员登录
- 账号信息先保存在 JSON 中，不使用数据库

### 6.2 登录后左侧边栏

从上到下为：

- 数据查询
- 性能展示
- 数据库总览
- 数据库文件管理
- 日志
- 用户管理

权限规则：

- 普通用户可见：数据查询、性能展示
- 管理员可见：全部

## 7. 功能说明

### 7.1 数据查询

界面要素：

- 数据库下拉框
- “数据库选中”按钮
- 查询关键字输入框
- “查找”按钮
- “全部”按钮
- 文件列表展示区

流程：

1. 用户选择数据库
2. 点击“数据库选中”
3. 输入关键字后点击“查找”，展示对应数据库中名字相关的文件条目
4. 不输入关键字点击“全部”，展示该数据库全部文件条目
5. 每个文件条目后附带“下载”按钮
6. 点击下载后：
   - 页面状态显示“等待下载”
   - 客户端启动恢复逻辑
   - 恢复完成后状态变成“下载完成”
   - 文件既保存到本地恢复目录，也通过 HTTP 返回浏览器下载

性能展示界面当前阶段先不做。

### 7.2 数据库总览

总览模块中分三个子界面：

- 总览
- 数据库添加
- 数据库文件管理

#### 总览子界面

界面要素：

- 数据库下拉框
- “文件”按钮
- “删除”按钮
- 文本展示框

功能：

- 点击“文件”显示该数据库中所有文件名
- 点击“删除”弹确认窗
- 确认后删除客户端和服务端相关数据库目录与数据

#### 数据库添加子界面

界面要素：

- 已有数据库展示框
- 新数据库名输入框
- “添加”按钮
- 文件上传区域
- “预处理”按钮
- 进度条占位

功能：

1. 输入新数据库名字
2. 检查不与已有数据库重名
3. 点击添加后，客户端和服务端创建对应空目录
4. 上传多个文件或整个平铺文件夹中的文件
5. 浏览器先传给客户端，再由客户端转发给服务端
6. 服务端保存原始文件
7. 点击“预处理”后启动打包和预处理
8. 成功后通知浏览器并刷新数据

说明：

- 当前阶段上传内容内部不允许带子文件夹
- 二阶段再考虑已有数据库追加和删除文件

#### 数据库文件管理子界面

当前阶段先占位，不实现。

后续目标：

- 选数据库
- 显示所有文件
- 每个文件后附带删除按钮

### 7.3 日志

分两个上标签：

- 查询日志
- 数据库日志

查询日志记录：

- 用户昵称
- 用户 ID / 账号
- 查询了哪个数据库
- 查询了哪个文件
- 查询时间

数据库日志记录：

- 数据库新建
- 数据库删除
- 相关时间

当前阶段可以先简单记录，分页与完整页面细节后补。

### 7.4 用户管理

管理员界面。

功能包括：

- 用户列表展示
- 删除用户
- 重置密码
- 添加新用户

存储方式：

- JSON 文件

## 8. 文件流设计

### 8.1 原始文件上传

服务端数据库原始文件目录：

`rust_data/server-data/<db_name>/data`

流程：

1. 浏览器上传文件到客户端 FastAPI
2. 客户端 FastAPI 将文件转发到服务端 FastAPI
3. 服务端 FastAPI 保存原始文件到对应数据库的 `data` 目录

### 8.2 打包输出

服务端打包输出目录：

`rust_data/server-data/<db_name>/snapshot`

打包输出文件：

- `data.bin`

### 8.3 manifest

manifest 当前阶段由客户端持有，作为权威文件级描述信息。

客户端 manifest 目录：

`rust_data/client-data/manifest`

文件命名：

- `<db_name>_manifest.json`

当前阶段约定：

- 服务端生成 manifest 后回传给客户端
- 客户端保存为本地 manifest

### 8.4 恢复中间块

客户端临时块目录：

`rust_data/client-data/data_item`

用于保存 `uread` 恢复出的块文件：

- `block_<index>.bin`

### 8.5 最终恢复文件

最终恢复目录：

`recover-file/<db_name>/`

恢复后文件保存到这里，并且同时通过客户端 API 回传浏览器下载。

## 9. 协议流设计

### 9.1 预处理

预处理是一次性任务，不是常驻服务。

包含：

- 服务端运行 `sprep`
- 客户端运行 `uprep`

任务完成后进程退出。

### 9.2 在线读

在线读服务为常驻服务：

- 服务端运行 `sread`

在需要恢复文件时，客户端启动：

- `uread`

### 9.3 恢复逻辑

当前有两种恢复方式：

#### PIR 恢复

流程：

1. 客户端读取本地 manifest
2. 根据 `file_id` 找到文件所需块号
3. 清理 `data_item` 目录
4. 拉起 `uread`
5. `uread` 从 `sread` 恢复对应块
6. Python 按顺序拼接块
7. 按 `size_bytes` 截断尾部补零
8. 校验 `sha256`
9. 保存恢复文件
10. 回传浏览器下载

#### Direct 恢复

流程：

1. 客户端读取本地 manifest
2. 直接从服务端数据库对应的 `snapshot/data.bin` 位置读取块
3. 按顺序拼接
4. 截断尾部补零
5. 校验 `sha256`
6. 保存恢复文件

说明：

- direct 恢复不调用 PIR 协议
- 主要用于文件打包与恢复链路校验

## 10. 当前目录与几何约束

### 10.1 文件目录约束

当前版本约定：

- 一个数据库的原始文件目录是平铺目录
- 不支持二级文件夹
- 因此 `file_id = 文件名`

### 10.2 数据几何

当前协议几何：

- 块大小：`256KB`
- 块数：`256`

这两个参数必须配置化，不允许写死在业务代码中。

## 11. FastAPI 组织方式

当前阶段：

- 客户端 API 和服务端 API 放在同一个 Python 主程序中
- 分别监听不同端口
- 后期再拆分部署

当前原则：

- Rust 不再改
- FastAPI 负责拉起 Rust 进程
- Python 通过端口探测或日志判断 ready

## 12. 第一阶段必须跑通的主链

第一阶段只要求跑通以下闭环：

1. 登录
2. 新建数据库
3. 上传原始文件到服务端
4. 服务端打包 `data.bin`
5. 服务端返回 manifest
6. 客户端保存 manifest
7. 运行 `sprep + uprep`
8. 启动 `sread`
9. 浏览器查询文件列表
10. 客户端发起 `restore/pir`
11. 恢复文件
12. 浏览器下载恢复结果

## 13. 第二阶段功能

第二阶段再补：

- 性能展示
- 数据库文件管理
- 已有数据库追加 / 删除文件
- 更完整日志
- 用户管理完善
- 双机部署
- 更完善的任务状态和进度展示

## 14. 当前开发原则

- 先保证主链跑通
- 不提前做大而全平台
- 不改 Rust 核心协议
- Python 承担控制层
- 页面优先做能操作主链的最小功能

