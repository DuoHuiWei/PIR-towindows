# Pirex 项目需求分析文档

## 文档信息

- **项目名称**: Pirex - Client-Efficient Online-Offline Private Information Retrieval
- **文档版本**: v1.0
- **创建日期**: 2025-01-XX
- **文档类型**: 需求分析文档

---

## 1. 项目概述

### 1.1 项目定位

Pirex 是一个**私有信息检索（PIR）中间件/服务层**项目，提供隐私保护的查询能力。

**核心特征**：
- ✅ **不是**完整的数据库系统（无 SQL、索引、事务等）
- ✅ **是**数据库层面开发中的**查询服务层**
- ✅ 位于应用层和存储层之间的中间件
- ✅ 提供隐私保护的查询能力

### 1.2 项目架构定位

```
┌─────────────────────────────────────────┐
│  应用层（Application Layer）              │
│  - Web 应用、移动应用                     │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  PIR 服务层（本项目）                      │
│  - 隐私保护查询服务                       │
│  - 加密计算                               │
│  - RESTful API（Axum）                   │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  数据库系统层（Database System Layer）    │
│  - PostgreSQL / MySQL / MongoDB        │
│  - 或文件系统 / 对象存储                   │
└─────────────────────────────────────────┘
```

### 1.3 技术栈

- **语言**: Rust
- **加密库**: libsecp256k1 (Bitcoin Core)
- **网络**: TCP Stream
- **存储**: 文件系统（内存映射）
- **性能优化**: SIMD、多线程、AVX2/AVX512

---

## 2. 项目结构分析

### 2.1 目录结构

```
pirex-main/
├── src/                    # Rust 源代码目录
│   ├── libs.rs            # 核心库：加密、存储、工具函数
│   ├── pirex_*.rs         # Pirex 方案的客户端/服务器实现
│   ├── pirexx_*.rs        # Pirex+ 方案的客户端/服务器实现
│   ├── helper.rs          # 数据初始化辅助工具
│   ├── elgamal.rs         # ElGamal 加密实现
│   └── build.rs           # 构建脚本（链接 secp256k1）
├── secp256k1/             # 自定义的 secp256k1 加密库
├── utils/                 # C++ 辅助函数
│   ├── helper.cpp         # 性能优化辅助函数
│   └── random.h           # 随机数生成
├── results/               # 性能测试结果输出目录
├── config.py              # 配置脚本（设置数据库大小和块大小）
├── control.py             # 自动化测试脚本
├── Cargo.toml             # Rust 项目配置
└── README.MD              # 项目文档
```

### 2.2 核心组件

#### 两个主要方案

**Pirex 方案**：
- `pirex_uprep`：客户端离线预处理
- `pirex_sprep`：服务器离线预处理
- `pirex_uread`：客户端在线查询
- `pirex_sread`：服务器在线查询

**Pirex+ 方案**：
- `pirexx_uprep`：客户端离线预处理
- `pirexx_sprep`：服务器离线预处理
- `pirexx_uread`：客户端在线查询
- `pirexx_sread`：服务器在线查询

#### 核心库 (`src/libs.rs`)

- `Crypto`：加密操作（AES-128、密钥生成、PIR 查询生成）
- `Storage`：服务器端数据存储与检索
- `StoragePlus`：Pirex+ 的增强存储
- `HintStorage`：提示存储（用于 Pirex+）

### 2.3 数据模型

- **数据库大小**: 2^N 条记录（N: 12-28）
- **记录大小**: 固定块大小（4KB、64KB、256KB）
- **存储方式**: 扁平数组结构，通过索引访问
- **数据格式**: 二进制块，无模式定义

---

## 3. 功能分析

### 3.1 核心功能

#### 离线预处理阶段
- 客户端生成密钥和提示
- 服务器预处理数据库
- 一次性操作，后续查询可复用

#### 在线查询阶段
- 客户端发送加密查询（隐藏查询索引）
- 服务器计算并返回结果（不知道查询了哪个索引）
- 客户端解密获取数据

#### 性能评估
- 测量计算延迟
- 测量带宽消耗
- 测量存储成本

### 3.2 当前功能限制

**不支持的功能**：
- ❌ SQL 查询语言
- ❌ 复杂查询条件（WHERE、JOIN 等）
- ❌ 数据更新操作
- ❌ 事务处理
- ❌ 索引管理
- ❌ 数据模型/Schema

**支持的功能**：
- ✅ 按索引查询单个记录
- ✅ 隐私保护（服务器不知道查询索引）
- ✅ 批量查询（通过多次调用）

---

## 4. 技术特性分析

### 4.1 性能特点

- **客户端高效**: 恒定入站带宽成本
- **低延迟**: 在线阶段延迟低
- **离线预处理**: 一次性预处理，后续查询快速
- **可扩展**: 支持 1GB 到 1TB 级数据库
- **灵活块大小**: 支持 4KB、64KB、256KB 等不同块大小

### 4.2 安全特性

- **隐私保护**: 服务器无法知道客户端查询了哪个索引
- **加密通信**: 使用 AES-128 和 secp256k1
- **零知识查询**: 查询过程不泄露信息

### 4.3 当前问题

1. **错误处理**: 大量使用 `expect()` 和 `unwrap()`（216 处）
2. **日志系统**: 仅使用 `println!`，无结构化日志
3. **配置管理**: 硬编码常量，通过 Python 脚本修改
4. **网络通信**: 简单 TCP，无加密、认证、超时
5. **代码质量**: 允许死代码、算术溢出等

---

## 5. 应用场景分析

### 5.1 主要应用领域

1. **医疗健康数据查询**
   - 患者查询电子病历、基因数据
   - 保护查询隐私，避免泄露疾病类型

2. **金融数据服务**
   - 查询股票价格、交易记录、信用报告
   - 隐藏查询意图，防止被分析

3. **地理位置服务**
   - 查询地图数据、POI、交通信息
   - 隐藏用户位置和查询意图

4. **学术与研究数据库**
   - 查询论文数据库、专利库
   - 保护研究方向和兴趣

5. **区块链与加密货币**
   - 查询区块链数据、交易历史
   - 隐藏查询的交易地址和意图

6. **云存储与文件系统**
   - 从云存储中检索文件
   - 不暴露文件名或路径

### 5.2 技术优势

- 客户端高效：恒定入站带宽成本
- 低延迟：在线阶段延迟低
- 离线预处理：一次性预处理，后续查询快速
- 可扩展：支持 1GB 到 1TB 级数据库
- 灵活块大小：支持不同块大小配置

---

## 6. 工程化需求分析

### 6.1 代码拆分需求

#### 当前问题
- 客户端和服务器代码耦合在同一个 `libs.rs` 中
- 硬编码配置（`SERVER_ADDRESS` 等）
- 网络协议耦合
- 文件系统耦合

#### 拆分方案：Workspace 架构

```
pirex-workspace/
├── Cargo.toml                    # Workspace 根配置
├── pirex-common/                 # 共享库 crate
│   ├── Cargo.toml
│   └── src/
│       ├── lib.rs
│       ├── types.rs              # 类型定义
│       ├── crypto.rs             # 加密操作（客户端+服务器共享）
│       └── protocol.rs           # 协议定义
├── pirex-client/                 # 客户端 SDK crate
│   ├── Cargo.toml
│   └── src/
│       ├── lib.rs                # 公开 API
│       ├── client.rs             # 客户端实现
│       └── storage.rs           # 客户端存储
├── pirex-server/                 # 服务器 crate
│   ├── Cargo.toml
│   └── src/
│       ├── lib.rs
│       ├── server.rs             # 服务器实现
│       └── storage.rs           # 服务器存储
└── pirex-api/                    # HTTP API 层（新增）
    ├── Cargo.toml
    └── src/
        └── main.rs               # Axum HTTP 服务器
```

### 6.2 API 设计需求

#### RESTful API vs FastAPI

**推荐方案：Axum (Rust) 实现 RESTful API**

**理由**：
1. **性能优势**: 零成本抽象，直接调用 Rust 核心库
2. **类型安全**: 编译期检查，减少运行时错误
3. **生态完整**: Tokio 生态，异步支持好
4. **维护性**: 统一技术栈，降低维护成本

**API 设计**：

```rust
POST /api/v1/query          // 单次查询
POST /api/v1/query/batch    // 批量查询
GET  /api/v1/health         // 健康检查
GET  /api/v1/status         // 服务器状态
```

### 6.3 工程化改进方向

#### 高优先级（生产就绪必需）
1. 错误处理与日志系统
2. 配置管理
3. 网络通信安全（TLS）
4. 测试框架
5. 监控与可观测性

#### 中优先级（提升质量）
6. 并发性能优化
7. API 设计与文档
8. 部署与运维
9. 代码质量改进

#### 低优先级（长期优化）
10. 多语言 SDK
11. 存储抽象
12. 性能深度优化

---

## 7. 与数据库系统集成分析

### 7.1 能否作为 MySQL 中间件？

**直接答案：不能直接使用**

**原因**：
1. **查询模式不兼容**
   - SQL: `SELECT * FROM table WHERE id = 123`
   - PIR: 直接查询索引 123
2. **数据模型不匹配**
   - MySQL: 表结构、行、列
   - PIR: 扁平数组、固定大小块
3. **隐私保护冲突**
   - 如果解析 SQL，服务器会知道查询条件，破坏隐私保护

### 7.2 集成方案

#### 方案一：索引映射方案（有限支持）
- 将 SQL 查询转换为索引查询
- 需要维护索引映射表
- **问题**: 隐私保护被削弱

#### 方案二：预索引方案（推荐）
- 预先将 MySQL 数据导出为 PIR 格式
- 建立 id -> index 映射表
- 运行时通过映射表查询

#### 方案三：混合架构（最佳实践）
- MySQL 存储元数据（非敏感字段）
- PIR 存储敏感数据
- 智能路由查询

---

## 8. 使用路径分析

### 8.1 路径一：直接使用二进制程序（最简单）

**步骤**：

```bash
# 1. 准备数据
python3 config.py 64 22
cargo build --release
./target/release/helper

# 2. 运行服务器
./target/release/pirex_sread

# 3. 运行客户端
./target/release/pirex_uread
```

**特点**：
- ✅ 最简单，无需改造
- ❌ 需要修改代码重新编译
- ❌ 不适合生产环境

### 8.2 路径二：封装为库（保持核心不变）

**架构**：
```
应用层
  ↓
PirexClientWrapper (包装层)
  ↓
原项目 Client (核心，不改动)
```

**特点**：
- ✅ 不改造核心逻辑
- ✅ 提供易用接口
- ✅ 适合 Rust 项目集成

### 8.3 路径三：HTTP API 包装层（推荐）

**架构**：
```
HTTP Client
  ↓ HTTP
Axum HTTP Server (包装层，新增)
  ↓ 直接调用
原项目核心 (不改动)
```

**实现要点**：
- 使用 Axum 实现 RESTful API
- 直接调用原项目的 `Client::access()` 方法
- 不改造任何核心 PIR 逻辑
- 读取原项目写入的结果文件

**API 示例**：
```rust
POST /api/v1/query
{
    "index": 123
}

Response:
{
    "data": "base64_encoded_data",
    "success": true
}
```

**特点**：
- ✅ 不改造核心
- ✅ 易用（HTTP 接口）
- ✅ 可扩展
- ✅ 适合生产使用

### 8.4 路径四：Python SDK 包装

**实现方式**：
- 通过 subprocess 调用原项目二进制
- 或通过 FFI 绑定 Rust 库

**特点**：
- ✅ 适合 Python 项目
- ✅ 不改造核心
- ⚠️ 性能有损失（如果通过 subprocess）

### 8.5 推荐使用路径

**优先级排序**：

1. **路径三：HTTP API 包装层** ⭐⭐⭐⭐⭐
   - 适合：生产使用、Web 应用
   - 实现时间：1-2 天
   - 优点：不改造核心、易用、可扩展

2. **路径二：封装为库** ⭐⭐⭐⭐
   - 适合：Rust 项目集成
   - 实现时间：1 周
   - 优点：类型安全、性能好

3. **路径一：直接使用二进制** ⭐⭐⭐
   - 适合：测试、演示
   - 实现时间：0（已可用）
   - 优点：最简单

4. **路径四：Python SDK** ⭐⭐⭐
   - 适合：Python 项目
   - 实现时间：1 周
   - 优点：易集成

---

## 9. 产品化路径分析

### 9.1 产品化方案对比

| 方案 | 适用场景 | 优点 | 缺点 | 推荐度 |
|------|---------|------|------|--------|
| **纯SDK方案** | 集成到现有系统 | 灵活、易集成、无额外部署 | 需要应用层开发 | ⭐⭐⭐⭐⭐ |
| **前后端分离** | 独立Web服务 | 用户友好、易用 | 需要前端开发、部署复杂 | ⭐⭐⭐ |
| **混合方案** | 通用产品 | 兼顾灵活性和易用性 | 开发工作量大 | ⭐⭐⭐⭐ |

### 9.2 推荐产品化架构

```
┌─────────────────────────────────────────────────────────┐
│  应用层（多种形式）                                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐            │
│  │ Web应用   │  │ 移动应用   │  │ 桌面应用   │            │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘            │
│       │            │            │                      │
│       └────────────┴────────────┘                      │
│                    │                                    │
└────────────────────┼────────────────────────────────────┘
                     │ 使用 SDK
┌────────────────────▼────────────────────────────────────┐
│  Pirex Client SDK (多语言绑定)                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐            │
│  │ Rust SDK │  │ Python SDK│  │ JS/TS SDK│            │
│  └──────────┘  └──────────┘  └──────────┘            │
└────────────────────┬────────────────────────────────────┘
                     │ 网络通信（HTTP/gRPC）
┌────────────────────▼────────────────────────────────────┐
│  Pirex Server (可选：HTTP API Gateway)                    │
│  ┌──────────────────────────────────────┐               │
│  │  HTTP/gRPC API Server                │               │
│  │  - RESTful API                       │               │
│  │  - gRPC Service                      │               │
│  │  - WebSocket (实时查询)               │               │
│  └──────────────┬───────────────────────┘               │
│                 │                                        │
│  ┌──────────────▼───────────────────────┐               │
│  │  PIR Core Engine (Rust)               │               │
│  │  - 查询处理                           │               │
│  │  - 加密计算                           │               │
│  └──────────────┬───────────────────────┘               │
└─────────────────┼────────────────────────────────────────┘
                  │
┌─────────────────▼────────────────────────────────────────┐
│  数据存储层                                                │
│  - 文件系统 / 数据库 / 对象存储                            │
└──────────────────────────────────────────────────────────┘
```

### 9.3 实施路线图

#### 阶段一：核心 SDK（2-3周）
1. 重构代码，提取核心库
2. 实现 Rust SDK API
3. 添加错误处理和日志
4. 编写单元测试

#### 阶段二：多语言绑定（2-3周）
1. 实现 FFI 层
2. Python SDK
3. JavaScript SDK
4. 编写示例代码

#### 阶段三：服务器 API（1-2周）
1. HTTP API 服务器（Axum）
2. gRPC 服务（可选）
3. 认证授权
4. 监控和日志

#### 阶段四：Web 前端（可选，2-3周）
1. 管理界面
2. 演示界面
3. 文档站点

#### 阶段五：部署和文档（1周）
1. Docker 容器化
2. 部署文档
3. API 文档
4. SDK 使用文档

---

## 10. 技术选型建议

### 10.1 Web 框架选择

**推荐：Axum (Rust)**

**对比**：

| 框架 | 性能 | 易用性 | 生态 | 推荐度 |
|------|------|--------|------|--------|
| **Axum** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Warp** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| **Actix-web** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **FastAPI** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

**选择理由**：
1. 性能：零成本抽象，直接调用 Rust 核心库
2. 类型安全：编译期检查
3. 生态：Tokio 生态，异步支持好
4. 维护：Tokio 团队维护，活跃

### 10.2 API 设计

**RESTful API 设计**：

```rust
// 健康检查
GET /api/v1/health

// 服务器状态
GET /api/v1/status

// 单次查询
POST /api/v1/query
{
    "index": 123,
    "database_id": "medical_records"
}

// 批量查询
POST /api/v1/query/batch
{
    "indices": [123, 456, 789],
    "database_id": "medical_records"
}
```

---

## 11. 关键设计原则

### 11.1 不改造核心原则

- ✅ **保持原项目的所有 PIR 核心逻辑不变**
- ✅ **只添加包装层和接口层**
- ✅ **不修改加密算法和查询协议**
- ✅ **保持原有的性能特性**

### 11.2 扩展性原则

- ✅ **可以添加 HTTP API 层**
- ✅ **可以添加多语言 SDK**
- ✅ **可以添加监控和日志**
- ✅ **可以添加配置管理**
- ✅ **可以添加错误处理**

### 11.3 兼容性原则

- ✅ **保持与现有二进制程序的兼容**
- ✅ **保持数据格式兼容**
- ✅ **保持网络协议兼容**

---

## 12. 风险评估

### 12.1 技术风险

1. **性能风险**
   - HTTP 包装层可能增加延迟
   - **缓解**: 使用异步框架，最小化开销

2. **兼容性风险**
   - 修改可能破坏现有功能
   - **缓解**: 严格遵循不改造核心原则

3. **安全风险**
   - 当前无 TLS 加密
   - **缓解**: 添加 TLS 支持

### 12.2 实施风险

1. **开发时间**
   - 完整产品化需要 2-3 个月
   - **缓解**: 分阶段实施，优先核心功能

2. **维护成本**
   - 多语言 SDK 增加维护负担
   - **缓解**: 优先 Rust SDK，其他按需

---

## 13. 总结与建议

### 13.1 项目定位总结

- **开发类型**: 数据库层面开发（Database Layer Development）
- **具体类别**: 数据库中间件/查询服务层
- **技术定位**: 查询服务层（Query Service Layer）
- **功能定位**: 隐私保护中间件（Privacy Middleware）

### 13.2 推荐实施路径

**短期（1-2周）**：
1. 实现 HTTP API 包装层（Axum）
2. 添加基本错误处理
3. 添加配置管理

**中期（1-2月）**：
1. 实现 Rust SDK
2. 实现 Python SDK
3. 添加监控和日志
4. 完善文档

**长期（3-6月）**：
1. 多语言 SDK（Go、Java 等）
2. Web 管理界面
3. 性能优化
4. 生产环境部署

### 13.3 关键成功因素

1. **保持核心不变**: 不改造 PIR 核心逻辑
2. **渐进式扩展**: 逐步添加功能，不一次性大改
3. **性能优先**: 保持原有的高性能特性
4. **文档完善**: 提供清晰的使用文档和 API 文档

---

## 附录

### A. 相关资源

- 项目仓库: [GitHub Repository]
- 论文: Client-Efficient Online-Offline Private Information Retrieval
- 文档: README.MD

### B. 术语表

- **PIR**: Private Information Retrieval（私有信息检索）
- **Pirex**: 基础 PIR 方案
- **Pirex+**: 改进的 PIR 方案（降低客户端存储成本）
- **离线预处理**: 一次性预处理阶段，生成密钥和提示
- **在线查询**: 实际的查询阶段，执行隐私保护查询

### C. 版本历史

| 版本 | 日期 | 修改内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2025-01-XX | 初始版本，完整需求分析 | - |

---

**文档结束**

