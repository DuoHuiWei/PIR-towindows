# 迁移到新电脑最小操作手册

## 适用前提

- 不改代码
- 不重新编译 Rust
- 直接使用现成 `.exe`
- 整个项目目录原样拷到新电脑

---

## 第 1 步：拷贝整个项目目录

### 前置检查

- 不要只拷 `rust_project`
- 要把整个项目根目录一起拷过去

### 至少应包含

- `rust_project`
- `rust_data`
- `client_ops`
- `server_ops`
- `static`
- `templates`
- `config.py`
- `main.py`

### 预期结果

- 新电脑上形成一个完整项目目录，例如：
  - `D:\PIR\new-work-v1`
  - `E:\Projects\new-work-v1`

---

## 第 2 步：修改 Python 根目录

### 前置检查

- 先确认新电脑上的项目根目录绝对路径

### 修改文件

- `config.py`

把这一行：

```python
WORKSPACE_ROOT = Path(r"E:\pir-worktrees\new-work-v1")
```

改成新电脑上的实际路径，例如：

```python
WORKSPACE_ROOT = Path(r"D:\PIR\new-work-v1")
```

### 预期结果

- Python 侧所有目录引用都改到新位置
- 包括：
  - 数据目录
  - 日志目录
  - 恢复目录
  - Rust 可执行文件目录

---

## 第 3 步：修改 pirex 的 Rust 运行根目录

### 修改文件

- `rust_project/config/pirex.json`

把：

```json
"workspace_root": "E:\\pir-worktrees\\new-work-v1"
```

改成新电脑上的实际路径，例如：

```json
"workspace_root": "D:\\PIR\\new-work-v1"
```

### 预期结果

- `pirex` 运行时会去新目录下找：
  - `rust_data\server-data`
  - `snapshot\data.bin`
  - `state-pirex`
  - 客户端导出目录

---

## 第 4 步：修改 pirexx 的 Rust 运行根目录

### 修改文件

- `rust_project/config/pirexx.json`

把：

```json
"workspace_root": "E:\\pir-worktrees\\new-work-v1"
```

改成新电脑上的实际路径，例如：

```json
"workspace_root": "D:\\PIR\\new-work-v1"
```

### 预期结果

- `pirexx` 运行时会去新目录下找：
  - `rust_data\server-data`
  - `snapshot\data.bin`
  - `state-pirexx`
  - 客户端导出目录

---

## 第 5 步：检查关键文件是否存在

### 前置检查

- 上面 3 处路径都改完以后，再做存在性检查

### 命令

```cmd
cd /d D:\PIR\new-work-v1
dir rust_project\config
dir rust_project\target\x86_64-pc-windows-gnu\debug
dir rust_data\server-data
dir rust_project\utils
```

### 预期结果

- 能看到：
  - `pirex.json`
  - `pirexx.json`
  - 各类 `*.exe`
  - `rust_data\server-data`
  - `utils` 目录

### 重点确认这些文件存在

- `pirex_sprep.exe`
- `pirex_uprep.exe`
- `pirex_sread.exe`
- `pirex_uread.exe`
- `pirexx_sprep.exe`
- `pirexx_uprep.exe`
- `pirexx_sread.exe`
- `pirexx_uread.exe`
- `helper.exe`
- `utils\dlp.bin`

---

## 第 6 步：做一次最小启动验证

### 前置检查

- 只先验证“能启动”
- 不急着马上跑完整测试

### 命令示例

```cmd
cd /d D:\PIR\new-work-v1\rust_project
target\x86_64-pc-windows-gnu\debug\pirex_sprep.exe test4096 127.0.0.1:18112
```

### 预期输出

```text
sprep: listening on 127.0.0.1:18112
```

### 这一步验证了什么

- Rust exe 能正常启动
- `pirex.json` 路径已生效
- 根目录迁移后没有把运行时路径搞坏

---

## 最小必改清单

迁到新电脑后，如果只是直接运行，不重新编译，最小只改这 3 处：

1. `config.py`
2. `rust_project/config/pirex.json`
3. `rust_project/config/pirexx.json`

---

## 什么时候还要改别的

### 需要重新编译 Rust

还要改：

- `rust_project/toolchain.toml`

### 需要换服务端口

需要改 Python 配置或启动参数。

### 新电脑缺 dlp.bin

需要补上：

- `rust_project/utils/dlp.bin`

### 新电脑缺 Python

需要先安装 Python。

---

## 一句话结论

如果你是整项目原样搬迁、直接跑现成 exe、不重新编译 Rust，那么迁到新电脑后通常只需要改：

- `config.py`
- `rust_project/config/pirex.json`
- `rust_project/config/pirexx.json`
