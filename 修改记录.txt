PIREXX 修改记录
编码：UTF-8
说明：每次本地 git 存档或 GitHub 提交后，在本文件追加记录。本文件同时作为本地工作树说明和仓库内的持续开发记录。

================================================================
记录时间：2026-04-08 16:54:43 +08:00
本地工作树：E:\pir-worktrees\bugfix-core-330be88
分支：codex/bugfix-core-330be88
当前阶段：构建底座三件套落地

本次修改产生的功能：
1. 新增 toolchain.toml，统一记录 Rust GNU nightly、cargo target、MinGW、CMake、secp256k1 构建目录等配置。
2. 新增 rebuild_secp256k1.cmd，提供 secp256k1 静态库重建入口，可重建 build-codex 中的 libsecp256k1.a 并覆盖到 .libs。
3. 新增 build_gnu_nightly.cmd，作为开发者主构建入口，负责：
   - 设置 Rust override
   - 检查 GNU target
   - 检查和提示 .libs 时间戳
   - 调用 rebuild_secp256k1.cmd
   - cargo clean
   - cargo build -j1 --target x86_64-pc-windows-gnu
4. 新增 load_toolchain_env.ps1，专门供 .cmd 脚本读取 toolchain.toml，避免 cmd 内联解析配置不稳定。
5. 更新 .gitignore，忽略 secp256k1/build-codex/ 生成目录，减少构建产物污染工作树。
6. 新增 构建底座三件套使用说明.md，补充主入口、底层入口、迁移到新机器时的使用方法。

本次涉及的文件：
- .gitignore
- toolchain.toml
- rebuild_secp256k1.cmd
- build_gnu_nightly.cmd
- load_toolchain_env.ps1
- 构建底座三件套使用说明.md

本次已完成验证：
1. build_gnu_nightly.cmd --check-only 可成功切到 nightly-2023-09-24-x86_64-pc-windows-gnu。
2. rebuild_secp256k1.cmd --no-configure 可成功重建并覆盖 .libs\libsecp256k1.a。
3. build_gnu_nightly.cmd 可成功完成 secp256k1 重建与 Rust GNU 构建，并生成：
   - target\x86_64-pc-windows-gnu\debug\pirexx_uread.exe
   - target\x86_64-pc-windows-gnu\debug\helper.exe

后续计划开发方向：
1. 基于当前构建底座，整合 0590bf5 的文件级 pack/restore 能力。
2. 在新整合工作树中引入 zero-full 修复后的核心协议逻辑。
3. 后续考虑增加对陈旧 .libs 的更强提示或来源校验，但当前先保持最小防错设计。
4. 将 direct restore / pir restore / pack dataset 等能力逐步整理成更统一的开发者或演示入口。

当前注意到的值得修改之处：
1. build.rs 当前只保留 .libs 链接 warning，后续可考虑加入更明显的“源码时间晚于链接库”的提示。
2. load_toolchain_env.ps1 当前为最小解析器，未来如配置文件扩展，可再加强 TOML 解析稳健性。
3. 目前 client_prep_auto、server_prep_auto、tmp/regression-core/ 仍是工作树噪声，不属于本次构建底座提交范围。
